[package]
name = "traffic-core"
version = "0.1.0"
edition = "2021"
authors = ["sing-scope Team"]
description = "High-performance analytics core for sing-box traffic"

[lib]
name = "traffic_core"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
